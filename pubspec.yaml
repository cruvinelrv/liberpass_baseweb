name: liberpass_baseweb
description: A new Flutter project.
publish_to: "none" 
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  liberpass_shared:
    git:
      url: https://github.com/cruvinelrv/lpass_shared.git
      ref: develop
  lpass_web_dependencies:
    git:
      url: https://github.com/cruvinelrv/lpass_web_dependencies.git
      ref: develop
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/images/logo_premier.png
    - assets/images/liber_pass.png
    - assets/translations/
